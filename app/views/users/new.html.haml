- provide(:title, 'Sign up')
.container 
	- if logged_in?
		%h1 Sign up
		.row
			.col-md-6.col-md-offset-3
				= form_for(@user, url: signup_path) do |f|
					= f.label :name
					= f.text_field :name
					= f.label :email
					= f.email_field :email
					= f.label :password
					= f.password_field :password
					= f.submit "Create my account", class: "btn btn-primary"
	- else 
		You are already logged in! To sign up for a new account, click #{link_to "here", logout_path, method: "delete"} to log out first .
		Otherwise, go back to the #{link_to "homepage", root_path}
